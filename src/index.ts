import { initializeAccordion } from './utils/accordion';
import { initializeVideoPlayer } from './utils/video-player';

// Call the accordion utility function, passing the selector for the accordion components
initializeAccordion('.accordion_component');

// Call the video player utility function, passing the selector for the video elements
initializeVideoPlayer('.us-vid-cover');
